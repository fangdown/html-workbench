# Run npm run setup to create local server settings.
HOST=127.0.0.1
PORT=3200
DATA_DIR=./data
# Set this to the exact browser origin when using an HTTPS reverse proxy.
# PUBLIC_ORIGIN=https://your-workbench.example.com
